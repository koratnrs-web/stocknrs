# การติดตั้งฐานข้อมูลสำหรับหน้า Settings

## 📋 ภาพรวม

หน้า Settings ตอนนี้มีการเชื่อมต่อฐานข้อมูลครบถ้วนแล้ว โดยใช้ตาราง `settings` และ `requesters` สำหรับเก็บการตั้งค่าต่างๆ ของระบบ

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง `settings`
เก็บการตั้งค่าต่างๆ ของระบบ แบ่งตามหมวดหมู่:

- **company**: ข้อมูลบริษัท (ชื่อ, อีเมล, เบอร์โทร, ที่อยู่, สกุลเงิน)
- **server**: การเชื่อมต่อเซิร์ฟเวอร์ (host, port)
- **database**: การเชื่อมต่อฐานข้อมูล (host, port, name, user)
- **email**: การตั้งค่าเซิร์ฟเวอร์อีเมล (host, port, type, encryption, auth)
- **approval**: การอนุมัติ (ชื่อผู้อนุมัติ, อีเมล, CC)
- **notification**: การแจ้งเตือน (สต็อกต่ำ, อีเมล)
- **system**: ระบบ (สำรองข้อมูลอัตโนมัติ)

### ตาราง `requesters`
เก็บรายชื่อผู้ขอหลายคน พร้อมข้อมูล:
- ชื่อผู้ขอ
- อีเมล
- แผนก/ฝ่าย
- สถานะการใช้งาน

## 🚀 การติดตั้ง

### วิธีที่ 1: ใช้ Script อัตโนมัติ (แนะนำ)

1. **รันไฟล์ batch บน Windows:**
   ```bash
   setup-settings-tables.bat
   ```

2. **กรอกข้อมูลการเชื่อมต่อฐานข้อมูล:**
   - Host: localhost (หรือ IP ของเซิร์ฟเวอร์)
   - Port: 5432 (หรือพอร์ตที่กำหนด)
   - Database: ชื่อฐานข้อมูล
   - User: ชื่อผู้ใช้

### วิธีที่ 2: รัน SQL Script เอง

1. **เปิดโปรแกรมจัดการฐานข้อมูล** (pgAdmin, DBeaver, หรือ psql)

2. **รันไฟล์ SQL:**
   ```sql
   \i setup-database-settings.sql
   ```

3. **หรือคัดลอกเนื้อหา** จากไฟล์ `setup-database-settings.sql` แล้วรันในโปรแกรม

## ✅ ฟีเจอร์ที่ใช้งานได้

### การตั้งค่าทั่วไป
- ✅ ข้อมูลบริษัท (ชื่อ, อีเมล, เบอร์โทร, ที่อยู่, สกุลเงิน)

### การตั้งค่าการเชื่อมต่อ
- ✅ การเชื่อมต่อเซิร์ฟเวอร์ (host, port)
- ✅ การเชื่อมต่อฐานข้อมูล (host, port, name, user)
- ✅ การตั้งค่าเซิร์ฟเวอร์อีเมล (Postfix + Dovecot, SMTP, Gmail, Outlook)
- ✅ การทดสอบการเชื่อมต่อแบบ Real-time

### การตั้งค่าการอนุมัติ
- ✅ รายชื่อผู้ขอหลายคน (เพิ่ม/ลบ/แก้ไข)
- ✅ ชื่อผู้อนุมัติและอีเมล
- ✅ อีเมลที่ CC

### การตั้งค่าการแจ้งเตือน
- ✅ แจ้งเตือนสต็อกต่ำ
- ✅ การแจ้งเตือนทางอีเมล

### การตั้งค่าระบบ
- ✅ การสำรองข้อมูลอัตโนมัติ
- ✅ ส่งออก/นำเข้าข้อมูล (JSON/CSV)
- ✅ ลบข้อมูลทั้งหมด

## 🔧 การแก้ไขปัญหา

### ข้อผิดพลาด "ตารางไม่พบ"
```sql
-- ตรวจสอบว่าตารางถูกสร้างแล้ว
\dt settings
\dt requesters

-- หากไม่มี ให้รัน SQL script อีกครั้ง
```

### ข้อผิดพลาดการเชื่อมต่อ
1. ตรวจสอบการตั้งค่า Supabase ใน `src/lib/supabase.ts`
2. ตรวจสอบ URL และ API Key
3. ตรวจสอบสิทธิ์การเข้าถึงฐานข้อมูล

### ข้อมูลไม่ถูกบันทึก
1. ตรวจสอบ Console ใน Developer Tools
2. ตรวจสอบ Network tab สำหรับ API calls
3. ตรวจสอบสิทธิ์การเขียนข้อมูลในฐานข้อมูล

## 📱 การใช้งาน

### เพิ่มผู้ขอใหม่
1. ไปที่แท็บ "การอนุมัติ"
2. คลิกปุ่ม "+ เพิ่มผู้ขอ"
3. กรอกข้อมูล: ชื่อ, อีเมล, แผนก
4. คลิก "บันทึกการเปลี่ยนแปลง"

### ทดสอบการเชื่อมต่อ
1. ไปที่แท็บ "การเชื่อมต่อ"
2. คลิกปุ่ม "🔍 ทดสอบการเชื่อมต่อ" สำหรับแต่ละบริการ
3. ระบบจะแสดงผลการทดสอบแบบ Real-time

### ส่งออกข้อมูล
1. ไปที่แท็บ "ระบบ"
2. คลิกปุ่ม "ส่งออก JSON" หรือ "ส่งออก CSV"
3. ไฟล์จะถูกดาวน์โหลด

## 🔄 การย้ายข้อมูล

ระบบจะย้ายข้อมูลจาก localStorage ไปฐานข้อมูลโดยอัตโนมัติ เมื่อ:
- โหลดหน้า Settings ครั้งแรก
- มีการบันทึกการตั้งค่าใหม่

## 📊 การตรวจสอบ

### ตรวจสอบข้อมูลในฐานข้อมูล
```sql
-- ดูการตั้งค่าทั้งหมด
SELECT * FROM settings ORDER BY category, key;

-- ดูผู้ขอทั้งหมด
SELECT * FROM requesters WHERE is_active = true;

-- ดูการตั้งค่าบริษัท
SELECT * FROM settings WHERE category = 'company';

-- ดูการตั้งค่าการเชื่อมต่อ
SELECT * FROM settings WHERE category IN ('server', 'database', 'email');

-- ดูการตั้งค่าเซิร์ฟเวอร์อีเมล
SELECT * FROM settings WHERE category = 'email';
```

## 🆘 การสนับสนุน

หากพบปัญหา:
1. ตรวจสอบ Console ใน Developer Tools
2. ตรวจสอบ Network tab สำหรับ API calls
3. ตรวจสอบการเชื่อมต่อฐานข้อมูล
4. รีเฟรชหน้าเว็บ

---

**หมายเหตุ**: การตั้งค่าทั้งหมดจะถูกบันทึกลงฐานข้อมูลและจะคงอยู่แม้จะรีเฟรชหน้าเว็บหรือปิดเบราว์เซอร์

## 📚 เอกสารเพิ่มเติม

- `POSTGRESQL_MIGRATION_GUIDE.md` - คู่มือการย้ายข้อมูลจาก SQLite ไป PostgreSQL
- `SUPABASE_TROUBLESHOOTING.md` - การแก้ไขปัญหา Supabase
- `EMAILJS_TROUBLESHOOTING.md` - การแก้ไขปัญหา EmailJS
- `POSTFIX_DOVECOT_SETUP.md` - คู่มือการตั้งค่า Postfix + Dovecot สำหรับใช้งานจริง
